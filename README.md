# VITyarthi-Project-Assignment
Write a Python program that repeatedly concatenates natural numbers (1, 2, 3, …) into a string until the length of the string becomes at least n. Then print exactly the first n characters of the string.
